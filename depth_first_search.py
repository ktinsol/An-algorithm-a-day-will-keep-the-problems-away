class DFS:
    def __init__(self):
        self.input = None
        self.search_value = None

    def set_input(self, input):
        self.input = input

    def set_search_value(self, value):
        self.search_value = value

    @staticmethod
    def dfs_algorithm():
        pass
        # Initialize needed vars
        visited = []

        # Define current node (start with root)

        # Check if search value == current node

        # If not find neighbors

        # For each neighbor: If neighbor not in visited then run dfs algo (recursion / backtracking) 
